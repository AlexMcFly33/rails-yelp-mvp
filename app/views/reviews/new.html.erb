<h1>Add a review</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, :collection => 0..5, :prompt => "Select your rating" %>
  <%= f.submit %>
<% end %>
<br>
<%= link_to 'BACK', restaurants_path, class: "btn btn-primary" %>
